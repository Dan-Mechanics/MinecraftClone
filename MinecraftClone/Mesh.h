#ifndef MESH_CLASS_H
#define MESH_CLASS_H

#include <string>

#include "vao.h"
#include "vbo.h"
#include "ebo.h"
#include "Camera.h"
#include "Texture.h"
#include <vector>

class Mesh {
public:
	// std::vector<Vertex> vertices;
	// std::vector<GLuint> indices;
	// std::vector<Texture> textures;
	// 
	// vao vao;
	// Mesh(std::vector())
};

#endif

